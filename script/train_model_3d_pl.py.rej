diff a/script/train_model_3d_pl.py b/script/train_model_3d_pl.py	(rejected hunks)
@@ -60,10 +60,14 @@
 # Pytorch Lightning
 # Train  ################################################################
 output_path = '../lightning'
+weight_path = '../model/facenet_3d_epoch3.ckpt'
 
 model = LightningSystem_2(net, data_dir, device, detector, img_num, img_size,
                           frame_window, batch_size, criterion)
 
+model.load_state_dict(torch.load(weight_path, map_location=torch.device(device))['state_dict'])
+
+
 checkpoint_callback = ModelCheckpoint(filepath='../lightning/ckpt', monitor='val_loss',
                                       mode='min', save_weights_only=True)
 trainer = Trainer(
